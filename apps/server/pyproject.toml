[tool.coverage.run]
branch = true
source = [ "server" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = "--cov --cov-report html:'../../coverage/apps/server/html' --cov-report xml:'../../coverage/apps/server/coverage.xml' --html='../../reports/apps/server/unittests/html/index.html' --junitxml='../../reports/apps/server/unittests/junit.xml'"

[tool.poetry]
name = "server"
version = "1.0.0"
description = "Automatically generated by Nx."
authors = [ ]
license = 'Proprietary'
readme = 'README.md'

  [[tool.poetry.packages]]
  include = "server"

  [tool.poetry.dependencies]
  python = ">=3.9,<3.11"

  [tool.poetry.group.dev.dependencies]
  autopep8 = "2.0.2"
  flake8 = "6.0.0"
  pytest = "7.3.1"
  pytest-sugar = "0.9.7"
  pytest-cov = "4.1.0"
  pytest-html = "3.2.0"

[tool.poetry.scripts]
start = "server.start:start"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
